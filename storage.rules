rules_version = '2';
service firebase.storage {
 match /b/{bucket}/o {
  // The server validates bytes, size, role and extraction. No direct browser upload bypass.
  match /{allPaths=**} { allow read, write: if false; }
 }
}
